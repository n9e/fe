import { isTimeSeries, isTimeSeriesArray } from './isTimeSeries';
import { isTableData, isTableDataArray } from './isTableData';

export { isTimeSeries, isTimeSeriesArray, isTableData, isTableDataArray };
