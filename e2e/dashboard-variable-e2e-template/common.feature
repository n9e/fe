# language: zh-CN

功能: 仪表盘变量 E2E 通用编写规则
  为了让 AI 可以稳定生成变量 E2E 用例
  也可以基于同一套标准 review 现有变量测试
  需要统一测试边界、术语和断言口径

  背景:
    假如 测试对象是“仪表盘变量”
    而且 使用全局默认测试上下文
    而且 如未单独说明则优先使用入口文档中的推荐命名约定
    而且 使用默认 dashboard 页面
    而且 测试应基于真实页面交互完成
    而且 所有变量测试都不点击仪表盘级“保存”
    而且 允许在变量编辑弹窗内点击“保存”使配置在当前会话内生效
    而且 渲染测试和交互测试默认创建一个“文本卡片” panel
    而且 文本卡片 panel 的“内容”通过变量 name 引用待测变量
    而且 变量 name 用于引用
    而且 变量 label 是“变量别名”，只影响界面显示，不影响 ${name} 取值
    而且 使用默认 localStorage 清理规则

  场景大纲: 统一变量元信息
    假如 变量类型是 "<变量类型>"
    而且 变量名称 name 是 "<变量名称>"
    而且 变量别名 label 是 "<变量别名>"
    而且 是否隐藏是 "<是否隐藏>"
    而且 是否多选是 "<是否多选>"
    而且 是否包含全选是 "<是否包含全选>"
    而且 自定义全选值是 "<自定义全选值>"
    而且 默认值是 "<默认值>"
    而且 数据源类型是 "<数据源类型>"
    而且 数据源值是 "<数据源值>"
    而且 查询或定义表达式是 "<查询或定义表达式>"
    而且 正则过滤是 "<正则过滤>"
    而且 依赖变量是 "<依赖变量>"
    而且 下游影响变量是 "<下游影响变量>"
    而且 文本卡片 panel 内容模板是 "<panel内容模板>"
    那么 AI 生成用例时应复用这些字段
    而且 AI review 用例时应优先核对这些字段是否被覆盖

    例子:
      | 变量类型 | 变量名称 | 变量别名 | 是否隐藏 | 是否多选 | 是否包含全选 | 自定义全选值 | 默认值 | 数据源类型 | 数据源值 | 查询或定义表达式 | 正则过滤 | 依赖变量 | 下游影响变量 | panel内容模板 |
      | textbox | ident | 机器 | false | N/A | N/A | N/A | i-1 | N/A | N/A | N/A | N/A | 无 | 无 | ident=${ident} |

  场景: 通用新增测试边界
    假如 用户打开目标仪表盘编辑页
    而且 用户打开“变量列表”弹窗
    当 用户点击“添加变量”
    并且 用户选择变量类型
    并且 用户填写变量名称、变量别名和类型特有字段
    并且 用户在变量编辑弹窗内点击“保存”
    那么 变量列表中应出现目标变量
    而且 变量类型应显示正确
    而且 当前步骤只验证变量新增成功
    但是 不验证仪表盘级保存

  场景: 通用真实数据约束
    假如 测试需要数据源、候选项或依赖联动数据
    当 AI 设计或生成 E2E 用例
    那么 应优先从默认 dashboard 页面和真实接口中获取数据
    而且 可参考 "e2e/dashboard/query-variable.spec.ts" 先校验真实数据源是否存在
    而且 不应通过 mock route 伪造候选项、数据源或变量依赖关系
    而且 Query 类型变量的候选数据应优先从真实 Prometheus series 数据中提取

  场景: 通用编辑测试边界
    假如 当前会话中已存在目标变量
    当 用户打开变量编辑弹窗并修改一个目标字段
    并且 用户在变量编辑弹窗内点击“保存”
    那么 再次打开变量编辑弹窗时该字段应正确回显
    而且 变量列表摘要信息应同步更新
    而且 当前会话内相关 UI 应按新配置生效
    但是 不允许触发仪表盘级保存

  场景: 通用渲染测试边界
    假如 当前会话中目标变量已完成变量弹窗内保存
    而且 已创建一个“文本卡片” panel
    当 页面完成渲染
    那么 变量栏中应展示该变量控件
    而且 控件值应与当前变量值一致
    而且 文本卡片 panel 中应渲染出通过 ${name} 替换后的内容
    而且 如果配置了变量别名 label 则变量栏应显示别名
    但是 panel 中仍必须通过 ${name} 取值

  场景: 通用交互测试边界
    假如 页面已渲染目标变量和“文本卡片” panel
    当 用户执行输入、选择、清空、回车或失焦操作
    那么 变量当前值应更新为预期值
    而且 控件显示值应与内部值一致
    而且 文本卡片 panel 内容应同步更新
    而且 如果该变量会触发查询或模板替换则相关结果应同步更新

  场景: 通用联动测试边界
    假如 当前变量依赖上游变量或被下游变量依赖
    而且 上下游变量存在可区分的数据集
    而且 文本卡片 panel 可同时展示上下游变量值
    当 用户修改上游变量值
    那么 下游变量候选项应刷新
    而且 下游变量当前值应按预期重置、保留或回退
    而且 panel 中应展示联动后的最终值

  场景: 通用会话内回显测试边界
    假如 用户已完成变量编辑并在变量弹窗内保存
    当 用户关闭变量编辑弹窗并重新打开
    那么 defaultValue、regex、multi、allOption、allValue、hide、label 等配置应正确回显
    而且 页面中的变量栏和文本卡片 panel 应继续按当前会话配置渲染
    但是 不验证 dashboard 级持久化

  场景: 通用异常与边界测试边界
    假如 测试覆盖异常路径
    那么 必填字段为空时应禁止保存并展示校验信息
    而且 非法正则输入时应展示错误提示
    而且 默认值不在候选项内时应验证回退逻辑
    而且 清空变量时应验证空字符串、空数组或 undefined 的状态和渲染行为
    而且 候选项为空时应验证空态展示和下游影响
    而且 名称重复或格式非法时应验证校验提示

  场景: Feature 结束后清理环境
    假如 一个 feature 文件中的所有场景都已执行完成
    当 测试准备结束该 feature
    那么 应删除该 feature 创建的变量、panel 和临时配置
    而且 应逐个清空该 feature 涉及变量的 localStorage
    而且 应按默认 localStorage 清理规则逐个清理
    而且 下一个 feature 必须从干净环境开始

  场景: 场景之间无关联时单独清理环境
    假如 同一个 feature 中相邻两个场景没有上下文依赖
    当 当前场景执行完成
    那么 应立即删除该场景创建的变量、panel 和临时配置
    而且 应立即清空该场景涉及变量的 localStorage
    而且 应按默认 localStorage 清理规则逐个清理
    而且 下一个独立场景必须从干净环境开始
